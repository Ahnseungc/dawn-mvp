import React from "react";

const Avbart_plus_sex_radiogroup = ({ label, children }) => {
  return (
    <fieldset>
      <legend>{label}</legend>
      {children}
    </fieldset>
  );
};
export default Avbart_plus_sex_radiogroup;
